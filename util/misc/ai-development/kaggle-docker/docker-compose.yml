version: '3'
services:
  kaggle:
    image: gcr.io/kaggle-gpu-images/python
    ports:
      - 8888:8888
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
